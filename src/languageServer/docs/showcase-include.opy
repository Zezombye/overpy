# =====================================================================
#  Sibling file pulled in by feature-showcase.opy via #!include.
#  Its only job is to give the DOCUMENT LINKS provider a real,
#  resolvable target to navigate to.
# =====================================================================

# COMMENT DOCS: this description shows up on hover / completion of
# `includedGreeting`, just like declarations in the main file.
globalvar includedGreeting
