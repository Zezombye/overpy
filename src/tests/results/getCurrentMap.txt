variables {
    global:
        0: A
        1: B
}
//Optimize for size enabled
rule ("test current map workaround for colosseo/esperança/samoa") {
    event {
        Ongoing - Global;
    }
    actions {
        Set Global Variable(A, First Of(Filtered Array(Array(Map(Samoa), Current Map), Compare(Custom String("{0}", Current Map), ==, String Split(Current Array Element, Empty Array)))));
        Set Global Variable(B, Compare(Global.A, ==, Map(Samoa)));
    }
}

